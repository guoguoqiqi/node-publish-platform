const config = {
    // 启动端口
    port: 4001,

    // 数据库配置
    database: {
        DATABASE: 'node_notes',
        USERNAME: 'root2',
        PASSWORD: 'root',
        PORT: '3306',
        HOST: 'localhost'
    }
}
module.exports = config